import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  display: "swap",
});

export const metadata = {
  title: "TheAdroitDev | Digital Agency | Websites, Ads & Growth",
  description: "We build websites and run ad campaigns on YouTube, Instagram & Google. Your complete digital growth partner.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body className={`${inter.className} antialiased`}>
        {children}
      </body>
    </html>
  );
}
